name: pages

on:
  workflow_run:
    workflows:
      - "tests"
    types:
      - completed
    branches:
      - "main"

jobs:
  pages:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Download code coverage report
        uses: actions/download-artifact@v6
        with:
          name: code-coverage-report-3.14
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Download code coverage badge
        uses: actions/download-artifact@v6
        with:
          name: coverage-badge-3.14
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: List everything
        shell: bash
        run: ls -al *

      - name: Extract code coverage badge
        uses: ihiroky/extract-action@v1
        with:
          file_path: code-coverage-report-3.14
          extract_dir: pages/

      - name: Extract code coverage report
        uses: ihiroky/extract-action@v1
        with:
          file_path: coverage-badge-3.14
          extract_dir: pages/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: pages

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
